description: Pointer sensitivity
compatible: "zmk,behavior-p2sm-sens"
include: two_param.yaml

properties:
  # must be a factor of 1000: step = percents * 10
  # valid values:
  #   1, 2, 4, 5, 8, 10, 20, 25, 40, 50, 100, 125, 200, 250, 500
  step:
    type: int
    required: true
    default: 125
